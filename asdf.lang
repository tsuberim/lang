import { +, print, &>, readFile, toStr, writeFile, join, map, length, split} from 'std'
import {y} from 'asdf2'


x = 4 + y
y = (
    x <- print('Reading')
    tsconfig <- readFile('./tsconfig.json')
    y <- writeFile('./tsconfig2.json', join(map(split(tsconfig, ','), \x -> toStr(length(x))), ',') )
    print('Done')
)